from collections import Counter
class Solution:
    def firstUniqChar(self, s: str) -> int:
        b=dict(Counter(s))
        for i,j in b.items():
          if j==1:
             return s.index(i)
        return -1

class Solution:
    def __init__(self):
        self.hasPathSum = lambda r, t: bool(r and (not (r.left or r.right) and t == r.val or self.hasPathSum(r.left, t - r.val) or self.hasPathSum(r.right, t - r.val)))

